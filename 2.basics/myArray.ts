const superHeroes: string[] = [];
// const herPower: number[] = [];
const herPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 255, 255]];

superHeroes.push("Spidey");
herPower.push(10);

allUsers.push({ name: "alvitto", isActive: true });
